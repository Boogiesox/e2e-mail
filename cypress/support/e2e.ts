// Register mail.tm custom commands
// This imports from the e2e-mail library and registers the cy.getMailMessages() command
import "e2e-mail/cypress";

// Example: Uncomment to disable uncaught exception handling if needed
// Cypress.on('uncaught:exception', (err, runnable) => {
//   return false
// })
